<div class="rangeParent">
    <label for="radiusProx">{{$label}}</label>
    <input class="radiusRange" type="range" id="radiusProx" name="{{$name}}" min="{{$min}}" max="{{$max}}" value="{{$value}}" step="{{$step}}">
    <div class="currentRadius"></div>
</div>
