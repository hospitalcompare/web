$(document).ready(function () {
// Initialise the bootstrap slider
    $("#radiusProx").slider({
        ticks: [1, 2, 3, 4, 5, 6, 7],
        ticks_labels: ['5', '10', '25', '50', '75', '100', 'England'],
        handle: 'square',
    });
});
